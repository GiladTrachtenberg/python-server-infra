apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: demo-pg
  namespace: demo
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  instances: 1
  bootstrap:
    initdb:
      database: demo
      owner: app
      secret:
        name: cnpg-app-creds
  storage:
    size: 1Gi
